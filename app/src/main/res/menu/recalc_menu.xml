<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/removeItems"
        android:icon="@drawable/ic_delete_black_24dp"
        android:title="@string/removeAll"
        app:showAsAction="ifRoom|withText" />
    <group
        android:id="@+id/menu_group_scan_by"
        android:checkableBehavior="single"
        android:visible="true">
        <item
            android:id="@+id/menu_scan_barcode"
            android:checked="true"
            android:title="@string/barcode" />
        <item
            android:id="@+id/menu_scan_plu"
            android:enabled="false"
            android:title="@string/plu" />
        <item
            android:id="@+id/menu_scan_code"
            android:enabled="false"
            android:title="@string/code" />
    </group>
    <group
        android:id="@+id/menu_group_mode"
        android:checkableBehavior="single"
        android:visible="true">
        <item
            android:id="@+id/menu_mode_auto"
            android:checked="true"
            android:enabled="true"
            android:title="@string/scanner" />
        <item
            android:id="@+id/menu_mode_manual"
            android:enabled="true"
            android:title="@string/manual" />
    </group>
</menu>